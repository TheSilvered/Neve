#ifndef NV_UTILS_H_
#define NV_UTILS_H_

#define NV_ARRLEN(arr) (sizeof(arr) / sizeof(*(arr)))
#define NV_MIN(a, b) ((a) < (b) ? (a) : (b))
#define NV_MAX(a, b) ((a) > (b) ? (a) : (b))

#endif // !NV_UTILS_H_
